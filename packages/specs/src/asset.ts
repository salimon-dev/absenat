export type AssetSchema = {
  id: string;
  frame: number;
  tags: string[];
  source: string;
};

export const AssetTags = ['grass', 'water', 'tile', 'tree', 'mushroom', 'sign', 'fire', 'bush', 'chest'];
